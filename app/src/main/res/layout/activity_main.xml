<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:background="@drawable/fundo_register">

    <EditText android:id="@+id/edtEmailLogin"
        android:layout_width="300dp"
        android:layout_height="50dp"

        android:hint="E-mail"
        android:textColorHint="@color/gray"
        android:paddingLeft="20dp"
        android:textColor="@color/black"
        android:background="@drawable/custom_input"
        android:inputType="textEmailAddress"

        android:layout_marginBottom="10dp"
        app:layout_constraintBottom_toTopOf="@id/edtSenhaLogin"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"/>

    <EditText android:id="@+id/edtSenhaLogin"
        android:layout_width="300dp"
        android:layout_height="50dp"

        android:hint="Senha"
        android:textColor="@color/black"
        android:textColorHint="@color/gray"
        android:paddingLeft="20dp"
        android:background="@drawable/custom_input"
        android:inputType="textPassword"

        android:layout_marginBottom="250dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"/>

    <Button android:id="@+id/btnEntrar"
        android:layout_width="150dp"
        android:layout_height="50dp"

        android:text="Entrar"
        android:textColor="#fff"
        android:textStyle="bold"
        android:background="@drawable/btn_roxo"

        android:layout_marginTop="70dp"
        app:layout_constraintTop_toBottomOf="@id/edtSenhaLogin"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        />

    <TextView android:id="@+id/lblRegistrar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"

        android:layout_marginTop="10dp"
        android:text="Cadastre-se"
        android:textColor="@color/white"
        android:textSize="20dp"
        android:textStyle="bold"

        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/btnEntrar" />




</androidx.constraintlayout.widget.ConstraintLayout>